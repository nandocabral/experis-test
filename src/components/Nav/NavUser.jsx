import React from "react";

const navUser = () => {
  const myStyle = {
    backgroundImage: `url(${""})`,
  };

  return (
    <div className="flex flex-row items-center">
      <div
        className="bg-center bg-contain bg-no-repeat w-full h-12 w-12 bg-white mr-2 rounded-full"
        style={myStyle}
      />
      Nando
    </div>
  );
};

export default navUser;
